{
  "name": "@perkcommons/data",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audit:sources": "tsx scripts/audit-sources.ts",
    "coverage": "tsx scripts/coverage.ts",
    "validate": "tsx scripts/validate.ts",
    "test": "npm run validate && tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "tsx": "^4.20.3",
    "typescript": "^7.0.2"
  }
}
